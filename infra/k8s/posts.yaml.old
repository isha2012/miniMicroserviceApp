apiVersion: v1
kind: Pod  #want to create a pod
metadata:
  name: posts #name of the pod
spec:
  containers:  #container that we want to use for creating a pod
    - name: posts    # - here means the array entity, we can have as many as container we need. 
      image: minmicroservice/posts:0.0.1
 